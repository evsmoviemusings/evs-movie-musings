backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "ticker"
    label: "Now Screening Ticker"
    files:
      - name: "ticker"
        label: "Ticker Items"
        file: "data/ticker.json"
        fields:
          - label: "Items"
            name: "items"
            widget: "list"
            summary: "{{label}}: {{detail}}"
            fields:
              - label: "Label (short, all caps look)"
                name: "label"
                widget: "string"
                hint: "e.g. NOW WATCHING, UP NEXT, ON THE SHELF"
              - label: "Detail"
                name: "detail"
                widget: "string"
  - name: "reviews"
    label: "Reviews"
    files:
      - name: "reviews"
        label: "All Reviews"
        file: "data/reviews.json"
        fields:
          - label: "Reviews"
            name: "reviews"
            widget: "list"
            summary: "{{title}} ({{category}})"
            fields:
              - label: "Movie Poster"
                name: "image"
                widget: "image"
                required: false
                hint: "Optional — upload a poster or still image for this review"
              - label: "Category"
                name: "category"
                widget: "select"
                options:
                  - { label: "Horror", value: "horror" }
                  - { label: "New Release", value: "new-release" }
                  - { label: "Old Release", value: "old-release" }
                  - { label: "Movie List", value: "movie-list" }
              - label: "Kicker (small label above the title)"
                name: "kicker"
                widget: "string"
              - label: "Movie Title"
                name: "title"
                widget: "string"
              - label: "Review Text"
                name: "body"
                widget: "text"
              - label: "Score"
                name: "score"
                widget: "string"
                default: "—"
                hint: "A number like 8, or — if you don't want to rate it yet"
              - label: "Score Unit"
                name: "scoreUnit"
                widget: "string"
                default: "/ 10"
                hint: "e.g. / 10, TBD, or titles"
              - label: "One-line Verdict"
                name: "verdict"
                widget: "string"
              - label: "Date Note"
                name: "date"
                widget: "string"
                hint: "e.g. Watched this week, Draft, Rewatched last month"
